<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Agenda de Citas</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
<nav class="navbar navbar-dark bg-dark mb-4 navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Agenda de Citas</a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarContent"
      aria-controls="navbarContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-between" id="navbarContent">
      <% if (user) { %>
        <form class="d-flex my-2 my-lg-0" role="search" action="/buscar" method="get">
          <input class="form-control me-2" type="search" name="q" placeholder="Buscar..." aria-label="Buscar">
          <button class="btn btn-outline-light" type="submit">Buscar</button>
        </form>

        <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center">
          <li class="nav-item">
            <a class="nav-link text-light" href="/lugares">Lugares</a>
          </li>
          <li class="nav-item me-2">
            <span class="navbar-text text-light small">
              Hola, <strong><%= user.username %></strong>
            </span>
          </li>
          <li class="nav-item">
            <a class="nav-link text-light" href="/cambiar-pass">Cambiar contraseña</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-light" href="/logout">Cerrar sesión</a>
          </li>
        </ul>
      <% } %>
    
        <div class="d-flex align-items-center ms-3 my-2 my-lg-0">
          <div class="form-check form-switch text-light mb-0">
            <input class="form-check-input" type="checkbox" id="themeToggle" />
            <label class="form-check-label small" for="themeToggle">Modo oscuro</label>
          </div>
        </div>
      </div>
  </div>
</nav>

<div class="container mb-5">
